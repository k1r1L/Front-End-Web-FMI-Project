const home = require('./home-controller');
const user = require('./user-controller');
const admin = require('./admin-controller');
const game = require('./games-controller');

module.exports = {
    home,
    user,
    admin,
    game
};